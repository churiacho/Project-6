module ApplicationHelper
    # def user_avatar(user, size=40)
    #     if current_user.avatar.attached?
    #         current_user.avatar.variant(resize: "100x100!")
    #     else
    #         gravatar_image_url(user.email, size: size)
    #     end
    # end
end
